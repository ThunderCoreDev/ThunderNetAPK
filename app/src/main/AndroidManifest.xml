<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thundernet.admin">

    <application
        android:name=".ThunderNetApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="ThunderNet Admin"
        android:theme="@style/Theme.ThunderNetAdmin">
        <activity android:name=".ui.SetupAdminActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" android:exported="true"/>
        <activity android:name=".MainActivity" android:exported="true"/>
    </application>

</manifest>